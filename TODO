 * Fiddle around with tcp_pre_analysis and improve scan detection/prevention
 * Remove unreferenced chains in chain.d/99...
   + parse build rulesfile and add -F / -X stanzas..?!

 * addService:
  - DNS
  + NSS hosts lockup fuer hostname

 * alff-cat:
  + Add TRAP to handle problems and restore old ruleset.
  + Add more trouble handling
  + Let alff-cat go to background after loading new rules.

 * Add pre-push-filter
