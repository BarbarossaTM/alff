 * Fiddle around with tcp_pre_analysis and improve scan detection/prevention
 * Remove empty chains in chain.d/99...
 * read *default* files before existing rules.d-file for a chain, to provide
   easy-to-use defaulting-ability and allow users to exclude chains from beeing
   defaulted by flushing the chain...
